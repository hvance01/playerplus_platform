{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd frontend && npm install -g pnpm && pnpm install && pnpm build && mkdir -p ../backend/internal/handler/dist && cp -r dist/* ../backend/internal/handler/dist/ && cd ../backend && go build -o server ./cmd/server"
  },
  "deploy": {
    "startCommand": "./backend/server",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
